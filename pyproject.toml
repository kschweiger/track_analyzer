[project]
name = "geo-track-analyzer"
description = "Analyze geospacial data tracks"
version = "3.0.0"
authors = [{ name = "Korbinian Schweiger", email = "korbinian.schweiger@gmail.com" }]
readme = "README.md"
license = { file = "LICENSE" }
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Topic :: Scientific/Engineering :: GIS",
  "Topic :: Scientific/Engineering :: Visualization",
  "Topic :: Software Development :: Libraries",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Programming Language :: Python :: 3 :: Only",
]
keywords = ["fit", "gpx", "visualization", "analysis"]
requires-python = ">=3.13"
dependencies = [
  "numpy>=2,<3",
  "gpxpy~=1.6",
  "requests>=2,<3",
  "pandas>=2,<3",
  "pydantic>=2,<3",
  "pydantic-numpy",
  "garmin-fit-sdk>=21.217.0,<22",
]

[project.urls]
Home = "https://github.com/kschweiger/track_analyzer"
Documentation = "https://kschweiger.github.io/track_analyzer/"
Source = "https://github.com/kschweiger/track_analyzer"

[project.scripts]
extract-fit-track = 'geo_track_analyzer:extract_track'
enhance-elevation = 'geo_track_analyzer:update_elevation'

[project.optional-dependencies]
cli = ["click>=8,<9", "rich"]
full = ["geo-track-analyzer[cli]"]
visualization = ["plotly>=5,<6"]

[build-system]
requires = ["uv_build>=0.12.19,<0.13"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""

[tool.pytest.ini_options]
testpaths = ["tests/"]

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "PD", "PT", "NPY", "N", "W", "RET", "ANN", "RUF", "SIM", "PERF"]
unfixable = ["F841", "RUF015"]
ignore = [
  "ANN202",
  "ANN205",
  "ANN001",
  "ANN002",
  "ANN003",
  "ANN401",
  "RET502",
  "RET503",
  "RET504",
  "RET505",
  "RUF005",
  "RUF009",
  "E731",
]
[tool.mypy]
strict_optional = true
check_untyped_defs = true
exclude = "tests"

[[tool.mypy.overrides]]
module = "fitparse.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "plotly.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "coloredlogs.*"
ignore_missing_imports = true

[tool.git-changelog]
output = "CHANGELOG.md"
convention = "conventional"
template = "path:.git-changelog/angular.md.jinja"

[dependency-groups]
dev = [
  "bpython>=0.25",
  "debugpy>=1.8.12",
  "pre-commit>=4.1.0",
  "ruff>=0.9.7",
  "uv>=0.6.2",
  "geo-track-analyzer[full]",
]
doc = [
  "mkdocs>=1.6.1",
  "mkdocs-click>=0.8.1",
  "mkdocs-material>=9.6.5",
  "mkdocstrings[python]>=0.28.2",
  "griffe>=0.46.0,<2.0", # version 2 does introduce issues with mkdocstrings[python]
]
test = [
  "pytest>=8.3.4",
  "pytest-cov>=6.0.0",
  "pytest-dependency>=0.6.0",
  "pytest-mock>=3.14.0",
  "pytest-retry>=1.7.0",
  "pytest-sugar>=1.0.0",
]
[tool.uv]
default-groups = ["dev", "test"]
